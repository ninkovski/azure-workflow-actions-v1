{
  "name": "notify-deployment",
  "version": "1.0.0",
  "description": "Reusable GitHub Actions for Azure deployments with email notifications",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "npx ncc build src/index.js -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "nodemailer": "^6.9.13"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
